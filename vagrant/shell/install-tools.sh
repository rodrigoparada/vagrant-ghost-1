#!/usr/bin/env bash

# Getting root
#sudo su

# Make sure repos are up to date
sudo apt-get update

# Install Tools necessary for downloading ghost
sudo apt-get install -y curl
sudo apt-get install -y unzip